query GetUser($id: ID!) {
    user(id: $id) {
        id
        name
        email
        posts {
            id
            title
        }
    }
}

query ListUsers {
    users {
        id
        name
    }
}
