directive @dummy_custom(value: String!) on FIELD

type Query {
    user(id: ID!): User!
}

type User {
    id: ID!
    name: String!
    profile: Profile
}

type Profile {
    bio: String
    avatar: String
}
