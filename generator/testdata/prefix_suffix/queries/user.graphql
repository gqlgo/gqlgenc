query GetUser($id: ID!) {
    user(id: $id) {
        id
        name
        email
    }
}

query ListUsers {
    users {
        id
        name
    }
}

mutation CreateUser($name: String!) {
    createUser(name: $name) {
        id
        name
    }
}
